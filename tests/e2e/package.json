{
  "name": "oyster-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests that drive the oyster web UI against a docker container on port 4000",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "record:videos": "node record-all-videos.mjs",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
