{
  "name": "oyster",
  "version": "0.1.0",
  "description": "Web UI for interacting with the pi coding agent remotely through a tunnel",
  "type": "module",
  "main": "server/server.mjs",
  "scripts": {
    "start": "node server/server.mjs",
    "dev": "vite --config vite.config.js",
    "build": "vite build --config vite.config.js",
    "build:pi": "cd pi && npx --no-install tsgo -p packages/tui/tsconfig.build.json && npx --no-install tsgo -p packages/ai/tsconfig.build.json && npm run build --workspace packages/agent && npm run build --workspace packages/coding-agent",
    "docs:build": "node scripts/build-docs.mjs",
    "preview": "vite preview --config vite.config.js",
    "migrate-app-data": "node scripts/migrate-app-data.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.35.0",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "katex": "^0.16.47"
  }
}
